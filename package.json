{
  "name": "firebolt",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna run start",
    "test": "lerna run test",
    "bootstrap": "lerna bootstrap --use-workspaces",
    "clean": "lerna clean",
    "build": "lerna run build"
  },
  "devDependencies": {
    "@types/faker": "^5.5.8",
    "@types/jest": "^27.0.2",
    "eslint": "^7.32.0",
    "jest": "^27.2.4",
    "lerna": "^4.0.0",
    "rimraf": "^3.0.2",
    "ts-jest": "^27.0.5",
    "typescript": "^4.4.3"
  }
}
