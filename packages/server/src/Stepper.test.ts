describe("start stepper engine", () => {
  test.todo(
    "Engine.start should generate a new token and return the first step"
  )

  test.todo(
    "Engine.start should use an existing token and retrieve the last uncompleted step"
  )
})

describe("next step handling", () => {
  test.todo("Engine.proceed should succesfully validate the step fields")

  test.todo(
    "Engine.proceed should validate the step fields and return an error"
  )
})

describe("Props presets apply", () => {
  test.todo(
    "props presets should be applied on Engine.start return value"
  )

  test.todo(
    "props presets should be applied on Engine.proceed return value"
  )

  test.todo(
    "props presets should be applied on Engine.goBack return value"
  )

  test.todo(
    "props presets should be applied on Engine.debug return value"
  )
})

describe("Stepper hooks working", () => {
  
})
