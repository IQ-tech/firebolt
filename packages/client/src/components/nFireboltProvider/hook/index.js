import { useRef } from "react";
import { createFireboltForm } from "@iq-firebolt/client-core";

function useFireboltProvider({formAccess, debug}) {

  return {

  }
}

export default useFireboltProvider;
