{
  "name": "@iq-firebolt/entities",
  "version": "0.7.0",
  "description": "> TODO: description",
  "author": "iq team",
  "homepage": "https://github.com/IQ-tech/firebolt#readme",
  "license": "Apache-2.0",
  "publishConfig": {
    "access": "public"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/IQ-tech/firebolt.git"
  },
  "scripts": {
    "generate-types": "node scripts/generate-types.js",
    "generate-schema": "ts-json-schema-generator --path 'types-test.ts' --type 'IExperienceJSONSchema' --no-type-check --out 'test.json'",
    "gen":"typescript-json-schema types-test.ts IExperienceJSONSchema --ignoreErrors --out test.json --jsdoc",
    "clear-dist": "rimraf dist",
    "test": "jest",
    "test:coverage": "jest --collect-coverage",
    "test:watch": "NODE_ENV=test jest --watchAll",
    "build": "yarn clear-dist && tsc",
    "clear-jest": "jest --clearCache"
  },
  "bugs": {
    "url": "https://github.com/IQ-tech/firebolt/issues"
  },
  "dependencies": {
    "@iq-firebolt/json-schema": "^0.7.0",
    "json-schema-to-typescript": "^11.0.1",
    "ts-json-schema-generator": "^1.0.0",
    "typescript-json-schema": "^0.53.1"
  }
}
