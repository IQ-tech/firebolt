// throw an error when providing a wrong access point
// throw an error on try to debug a step without debug key

describe("something", () => {
  test("asdsf", () => {
    expect("lala").toBeTruthy();
  });
});
